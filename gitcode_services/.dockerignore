# 凭证绝不进镜像层（运行时以 volume 挂载注入）
config.yaml
!config.yaml.example
.env
*.env

# 本地运行产物（通过 volume 挂载，不需要打进镜像）
phase02/runs/
phase02/reports/
phase02/.cache/

# Python 缓存
__pycache__/
*.py[cod]

# 版本控制与 IDE
.git/
.gitignore
.idea/
.vscode/

# 文档（不影响执行，减小镜像层）
JENKINS-SETUP.md
README.md
