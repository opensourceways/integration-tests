---
id: REL-OOM-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "内存耗尽/OOM — 大内存申请导致 runner 被 kill"
intent_ref: INTENT-REL-016
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
teardown:
  reset: fixture
