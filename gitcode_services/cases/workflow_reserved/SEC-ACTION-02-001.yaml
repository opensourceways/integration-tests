---
id: SEC-ACTION-02-001
dimensions: ["security"]
dimension: security
priority: P1
title: "第三方 action 的浮动 tag 被重写指向恶意 commit"
intent_ref: INTENT-SEC-014
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
  - type: negative
    target: run_logs
    must_not_contain: "UNEXPECTED_BEHAVIOR"
teardown:
  reset: fixture
