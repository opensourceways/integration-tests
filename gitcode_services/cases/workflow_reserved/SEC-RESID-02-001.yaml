---
id: SEC-RESID-02-001
dimensions: ["security"]
dimension: security
priority: P1
title: "Runner 非一次性导致前一个 job 的文件/环境变量残留"
intent_ref: INTENT-SEC-016
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
  - type: negative
    target: run_logs
    must_not_contain: "UNEXPECTED_BEHAVIOR"
teardown:
  reset: fixture
