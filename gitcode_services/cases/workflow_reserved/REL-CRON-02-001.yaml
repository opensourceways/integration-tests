---
id: REL-CRON-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "schedule cron 高频触发洪泛 — 1 分钟内 10 个 schedule workflow"
intent_ref: INTENT-REL-022
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
teardown:
  reset: fixture
