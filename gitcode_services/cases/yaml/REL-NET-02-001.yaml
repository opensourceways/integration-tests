---
id: REL-NET-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "网络分区 — job 执行中 runner 与平台控制面断连"
intent_ref: INTENT-REL-015
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
teardown:
  reset: fixture
