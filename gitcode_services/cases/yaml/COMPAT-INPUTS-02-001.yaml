---
id: COMPAT-INPUTS-02-001
dimensions: ["compatibility"]
dimension: compatibility
priority: P1
title: "workflow_dispatch inputs 类型限制为 string 在 UI 表单中的呈现"
intent_ref: INTENT-SPEC-022
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
