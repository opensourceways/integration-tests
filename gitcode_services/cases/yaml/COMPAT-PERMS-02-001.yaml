---
id: COMPAT-PERMS-02-001
dimensions: ["compatibility"]
dimension: compatibility
priority: P1
title: "permissions 权限域命名在 UI 中的展示与文档一致性"
intent_ref: INTENT-SPEC-025
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
