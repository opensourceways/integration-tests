---
id: USE-SUMMARY-02-001
dimensions: ["usability"]
dimension: usability
priority: P1
title: "Step summary Markdown 在运行详情页的渲染质量"
intent_ref: INTENT-USE-026
test_type: ui
setup:
  repo_fixture: null
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
