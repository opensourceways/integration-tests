---
id: COMP-DETAIL-02-001
dimensions: ["completeness"]
dimension: completeness
priority: P1
title: "运行详情页按 stages 渲染 Stage 侧栏、Job 卡片与 Step 时间线"
intent_ref: INTENT-SPEC-002
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
