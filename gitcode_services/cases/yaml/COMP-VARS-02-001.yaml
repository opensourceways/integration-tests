---
id: COMP-VARS-02-001
dimensions: ["completeness", "compatibility"]
dimension: completeness
priority: P1
title: "项目 Settings Variables 管理页面的创建、编辑与类型展示"
intent_ref: INTENT-SPEC-012
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
