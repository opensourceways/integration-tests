---
id: USE-DIFF-02-001
dimensions: ["usability"]
dimension: usability
priority: P1
title: "大 diff 文件在 MR 评审页的懒加载/分页与性能提示"
intent_ref: INTENT-USE-030
test_type: ui
setup:
  repo_fixture: null
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
