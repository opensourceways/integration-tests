---
id: COMP-CONC-02-001
dimensions: ["reliability", "completeness"]
dimension: completeness
priority: P1
title: "并发控制 concurrency.max 超出 1-5 范围时的报错或限制"
intent_ref: INTENT-SPEC-023
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
