---
id: REL-RERUN-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "Rerun 次数边界 — 单条失败运行最多重新运行 3 次"
intent_ref: INTENT-REL-007
test_type: ui
setup:
  repo_fixture: null
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
