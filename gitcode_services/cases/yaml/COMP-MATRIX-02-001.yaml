---
id: COMP-MATRIX-02-001
dimensions: ["completeness"]
dimension: completeness
priority: P1
title: "矩阵构建在 UI 中展开为多个 Job 实例并各自展示状态"
intent_ref: INTENT-SPEC-009
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
