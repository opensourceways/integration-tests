---
id: REL-CANCEL2-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "手动取消正在运行的 step — 验证清理与状态迁移"
intent_ref: INTENT-REL-012
test_type: ui
setup:
  repo_fixture: null
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
