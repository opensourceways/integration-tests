---
id: COMP-HOOK-02-001
dimensions: ["completeness"]
dimension: completeness
priority: P1
title: "Webhook 配置页面的创建、事件选择、签名密钥设置与测试投递"
intent_ref: INTENT-SPEC-015
test_type: ui
setup:
  repo_fixture: null
  secrets: []
ui:
  url: "https://gitcode.com/{owner}/{repo}/actions"
  viewport:
    width: 1280
    height: 720
  actions:
    - type: wait_for_selector
      selector: "body"
      timeout: 10000
assertions:
  - type: positive
    target: ui_element_visible
    selector: "body"
teardown:
  reset: none
