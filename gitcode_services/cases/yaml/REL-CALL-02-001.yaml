---
id: REL-CALL-02-001
dimensions: ["reliability"]
dimension: reliability
priority: P1
title: "workflow_call 嵌套层数边界 — 3 层嵌套"
intent_ref: INTENT-REL-008
test_type: workflow
setup:
  repo_fixture: default
  secrets: []
workflow: |
  name: test-run
  on:
    workflow_dispatch:
  jobs:
    test:
      name: test job
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: checkout
          uses: checkout
        - name: run test
          run: |
            echo "done"
trigger:
  event: workflow_dispatch
  as: maintainer
fault_injection: null
assertions:
  - type: positive
    target: run_status
    equals: "completed"
teardown:
  reset: fixture
